machine:
  node:
    version: 7.10.0
dependencies:
  override:
    - yarn
    - cd orchestrator && yarn install
    - cd client && yarn install  
test:
  pre:
    - NODE_ENV=test yarn start:
      background: true
    - sleep 5
  override:
    - yarn test