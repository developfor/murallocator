machine:
  node:
    version: 7.10.0
dependencies:
  override:
    - yarn
    - cd orchestrator && yarn install
    - cd client && yarn install
machine:
  post:
    - NODE_ENV=test yarn start:
      - background: true
    - sleep 5
test:
  override:
    - yarn test